# deckgen-local

Local cross-repo deck generation for Markdown, research packages, and learning notes.

The first supported output is an HTML preview deck. Editable PPTX export is routed through a configured local `ppt-master` checkout.
